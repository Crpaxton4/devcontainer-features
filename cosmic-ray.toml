[cosmic-ray]
module-path = "src"
timeout = 120.0
excluded-modules = ["**/tests/**", "**/examples/**"]
test-command = 'uv run python -m unittest discover -s tests -p "test_*.py"'

[cosmic-ray.distributor]
name = "local"
