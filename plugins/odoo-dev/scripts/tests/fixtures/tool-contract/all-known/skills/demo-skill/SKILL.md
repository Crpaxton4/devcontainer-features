# demo skill (fixture)

Read `mcp__odoo-mcp__timesheet_summary` first. The odoo-mcp `start_task` tool
cuts the branch; close out with `odoo-sdk cmd task_note`.

Not tool references: a `groups_id` field and the `tours_skipped` blocker.
