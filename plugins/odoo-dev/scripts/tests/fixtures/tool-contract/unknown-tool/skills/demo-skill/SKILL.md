# demo skill (fixture)

Call `mcp__odoo-mcp__frobnicate_widgets` to frobnicate, then post a note with
`odoo-sdk cmd task_note`. The odoo-mcp `bogus_bare` tool does the rest.

Not tool references: the `default_branch` key, a `view_mode` attribute, and
the wildcard `mcp__odoo-mcp__*` family mention.
